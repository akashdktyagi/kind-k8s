---
 apiVersion: v1
 kind: Pod
 metadata:
   name: springboot-ref-app
   labels:
     app: springboot-ref-app
 spec:
   containers:
     - name: springboot-ref-app
       image: yantraqa/spring-ref-app:latest
       ports:
         - containerPort: 9096